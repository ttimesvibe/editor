# LAB v2 FRESH — 2026-05-09 시작 (다 세션)

## 본 사료의 정체성

- **목적**: prod editor 의 v1→v2 evolution layers 를 걷어내고, **fresh v2** 를 설계+구현. v3/v4 evolution 의 디딤돌.
- **방법**: 사료 (`cms-v2-plan/` 188 개 .md + ttimes-* 8 개 옛 앱 + prod 코드) **전수 정독** → 본질 추출 → 효율화 설계 → 모듈별 구현. **빈 곳 0**.
- **속도**: "절대로 빠른 방법이 아니라 완벽한 최선의 방향" (사용자 명시 2026-05-09)
- **세션 분포**: 이번 세션 = 사료 정독 + 본 master doc 자라남. 추가 세션에서 implementation.
- **옵션 묻지 않음**: 사용자 명시 — "옵션을 주고 고르라고 묻지 말고, 전체 사료를 읽고 스터디하고 정리하면서 최선의 방향". 큰 분기점에선 사용자 검토 받지만, 작은 결정은 직접.

## 사용자 명시 약속 (이 사료의 헌장)

1. **전수 정독** — 188 개 + 8 옛 앱 + prod 코드. 빠뜨림 0
2. **빈 곳 0** — 모든 사료 내용이 구현 가능한 형태로 본 master doc 에 박제
3. **최선 vs 빠름** — 빠른 길 명시적 거부. 완벽한 최선
4. **사료 박제 디시플린** — 정독 결과 + 설계 결정 + 구현 계획 모두 본 사료에 누적

## 옛 사료 (이 작업 전)

- [`lab-setup-2026-05-09.md`](./lab-setup-2026-05-09.md) — 옛 계획 (코드 복사). Phase 0 인프라 박제 보존 (GitHub PAT / Cloudflare Worker `lab` / KV `lab-sessions` 등은 새 방향에서도 사용)
- prod editor 의 CHANGELOG #1~#16 — v2 진화 사료
- prod editor 의 README / CLAUDE.md — 현 상태 박제

## 인프라 (Phase 0 보존, 새 방향에도 사용)

| 레벨 | 값 |
|---|---|
| 로컬 폴더 | `D:\OneDrive - 머니투데이\개발\통합 개발\lab\` (현 빈 폴더) |
| GitHub | `github.com/ttimesvibe/lab` |
| Cloudflare Worker | `lab` (ttimesvibe), URL `https://lab.ttimes.workers.dev` |
| KV namespace | `lab-sessions` (id `fbb8da8adcae4ee0a555abff66f798ac`) |
| Worker bindings | KV `SESSIONS` → `lab-sessions` (옛 lab → SESSIONS 정정 완료) |
| Pages URL | `https://ttimesvibe.github.io/lab/` (예정) |

## 사용자 명시 결정 (옛 lab-setup 에서 가져옴)

| # | 결정 |
|---|---|
| 이름 | **`lab`** |
| KV 마이그레이션 | 90 keys 전부 |
| OPENAI/GEMINI 키 | prod 와 공유 |
| JWT_SECRET | **별도** (test 토큰이 prod 와 안 섞이게) |
| Pages | prod 동일 형식 |
| Phase 3 (옛 환경 정리) | 사용자 명시 "맨 마지막" — defer |

## 작업 단계 (이 사료가 자라며 명시화)

### Stage 0 — 본 사료 신설 + 사료 인벤토리 (현재)
- [x] 옛 ops 문서 superseded 표시
- [x] 본 master doc 신설 (이 문서)
- [ ] CHANGELOG #17 + CLAUDE.md 갱신
- [ ] commit + push (= pivot 박제)
- [ ] 사료 인벤토리 (188 개 .md 카탈로그) — 다음 단계

### Stage 1 — 사료 전수 정독 + 본질 추출
독서 우선순위 (자라며 갱신):
1. `_meta/CHARTER_11TABS_EQUAL_v1.md` — 11 탭 동등 헌장 (이번 세션 commit 들이 여러번 참조)
2. `_meta/00_MASTER_PLAN.md` — 마스터 플랜
3. `_meta/KEY_FINDINGS.md` — 주요 발견
4. `_meta/PROGRESS.md` — 진행 사료
5. `03_PRD/revised_PRD.md` — 1235 줄 PRD
6. `06_v2_finalization/_PLAN.md` — finalization plan
7. `06_v2_finalization/v2_patch_bundle.md` — 596 줄 패치 묶음
8. `06_v2_finalization/verification_checklist.md` — 430 줄 검증 항목
9. `06_v2_finalization/v2_release_notes.md`
10. `06_v2_finalization/regression_results.md`
11. `_meta/AUDIT_PLAN_VS_CODE_*.md` (3 개 v1/v2/v3) — 사료 vs 코드 audit
12. `_meta/MULTIUSER_100_SCENARIOS_*.md` — 다중 사용자 시나리오
13. `_meta/POSTMORTEM_*.md` — 사후 분석
14. `_meta/SESSION_HANDOFF_R_PHASE_*.md` — R Phase 인계
15. `_meta/THIRD_PARTY_REVIEW_*` — 외부 리뷰
16. `_meta/PROTOTYPE_SAVE_ENGINE` — 저장 엔진 프로토타입
17. `_meta/REGRESSION_TRACE_*` — 회귀 추적
18. `_meta/RESUME_GUIDE.md` — resume 가이드
19. `06_v2_finalization/code/` — v2 finalization 시점 코드 (5/6 즈음)
20. `06_v2_finalization/reviews/` — peer review
21. `01_known_issues/` ~ `05_full_audit/` — 단계별 사료 (~150 .md)
22. `archive/` — 옛 사료
23. `RESUME_AT_HOME_PC.md` / `RESUME_AT_OFFICE_PC_*.md` / `HANDOFF.md` — 세션간 인계
24. `ttimes-*/` 8 개 옛 앱 폴더
25. prod editor 코드 자체 (final reference)

### Stage 2 — 새 architecture 설계
사료 정독 결과 통합 → fresh v2 architecture (모듈/데이터 모델/API/배포 구조). 다음 영역 박제:
- 11 탭 정의 (CHARTER 기반)
- KV 스키마 (현 vs 새)
- Worker 모듈 분리
- Frontend state 관리
- 빌드/배포 흐름
- 헌장 정합 검증

### Stage 3 — 효율화 영역 (사료 정독 후 도출)
빠르게 결정 X. 사료 정독 후 자연스럽게 emerge.

### Stage 4 — 구현 계획 (모듈별)
사료 빈 곳 0 인 상태에서 수립.

### Stage 5 — 구현 (다 세션)

### Stage 6 — 라이브 검증

## 박제 영역 (자라남)

이 사료가 자라면서 다음 섹션들이 추가됨:

- **사료 인벤토리** — 188 + α 개 사료의 카탈로그 + 정독 상태
- **본질 추출** — 사료별 핵심 발췌 (CHARTER 의 11 탭 동등이란? PRD 의 핵심 요구? v2 patch 의 정확한 변경 범위?)
- **데이터 모델** — KV 스키마 / 인덱스 / 11 탭 별 데이터 형식
- **모듈 카탈로그** — Worker / Frontend 의 모듈 분리 안
- **API 카탈로그** — 모든 endpoint + request/response 스키마
- **기능 인벤토리** — 사료에 박힌 모든 기능 (가/이/와)
- **설계 결정 log** — 매 결정 + 근거 (사료 인용)
- **구현 계획** — 모듈별 / 단계별 작업 단위

## 사료 본질 추출 (자라며 누적)

### S1.2 — `_meta/00_MASTER_PLAN.md` (161 줄)

**작성**: 2026-05-04. 작업 폴더 구조 + 6 단계 작업 정의 + 컨텍스트 단절 대응.

#### 작업 목적 (사용자 명시)
"누더기 PRD 정리 + 저장 로직 등 구조적 결함 패치 → 내부 명명 **CMS v2**. 라이브 서비스 라벨링은 변경 X."

#### `cms-v2-plan/` 폴더 구조 (188 .md 의 조직)
```
cms-v2-plan/
├── _meta/                            ← SSOT (Single Source of Truth)
│   ├── 00_MASTER_PLAN.md             ← 이 파일
│   ├── PROGRESS.md                   ← 진행 상태 추적 (체크리스트)
│   └── RESUME_GUIDE.md               ← 컨텍스트 단절 시 재개 가이드
├── 01_known_issues/                  ← Step 1
│   ├── urgent_fixes.md
│   ├── critical_bugs.md
│   └── nice_to_have.md
├── 02_patch_plan/                    ← Step 2
│   ├── overview.md
│   ├── per_bug_patch.md
│   ├── architectural_changes.md
│   └── rollout_plan.md
├── 03_PRD/                           ← Step 3
│   ├── existing_PRD_audit.md
│   ├── update_history.md
│   └── revised_PRD.md
├── 04_structural_review/             ← Step 4
│   ├── crosscheck.md
│   └── revised_patch_plan.md
├── 05_full_audit/                    ← Step 5
│   ├── per_tab_review.md             (correction/guide/review/highlight/manuscript/subtitle/setgen/metadata/visual/modify/set)
│   ├── multi_user_concerns.md
│   ├── error_handling.md
│   └── perf_security.md
├── 06_v2_finalization/               ← Step 6
│   ├── v2_release_notes.md
│   ├── v2_patch_bundle.md
│   └── verification_checklist.md
├── source/                           ← ttimesvibe/editor 코드 사본 (분석용)
└── reports/                          ← 단계별 요약 보고서
```

#### 6 단계 작업 흐름 (각 단계 = 별 컨텍스트, 30-60 분)

| Step | 산출물 | 완료 조건 |
|---|---|---|
| 1 | `01_known_issues/` 3 개 파일 | 각 항목에 (증상/원인/위치/심각도) 명시 |
| 2 | `02_patch_plan/` 4 개 파일 | 패치별 (파일/라인/before-after pseudo-code) |
| 3 | `03_PRD/` 3 개 파일 | revised_PRD 가 현재 운영 코드와 일치 cross-check 통과 |
| 4 | `04_structural_review/` 2 개 | PRD 위반 0 건 |
| 5 | `05_full_audit/` 4 개 | 추가 이슈는 Step 1/2 환류 |
| 6 | `06_v2_finalization/` 3 개 | verification_checklist 모두 체크 가능 |

#### 컨텍스트 단절 대응 메커니즘 (사료의 디시플린)
1. **모든 산출물 즉시 파일 저장** — 메모리 의존 X
2. **각 단계 시작/종료 시 PROGRESS.md 갱신**
3. **하나의 단계 = 한 컨텍스트 안에 완결**
4. **단계 시작 시 이전 단계 산출물만 읽으면 충분** (의존성 최소화)

**재개 절차**:
1. `_meta/00_MASTER_PLAN.md` 읽기
2. `_meta/PROGRESS.md` 읽기
3. `_meta/RESUME_GUIDE.md` 읽기
4. 직전 완료 단계 산출물 한두 개
5. 다음 미완료 단계 진행

#### 작업 룰 (사용자 명시)
- **코드는 source/ 사본에서 분석**, 라이브 ttimesvibe/editor 직접 수정 X
- **사용자 확인 없이 라이브 운영 중인 시스템에 손대지 않음**
- **모든 가설은 코드 인용 또는 명시적 근거 표기**

#### lab-v2-fresh 작업과의 관계
MASTER_PLAN 의 6 단계는 prod editor 의 v2 진화 사료. lab fresh v2 는:
- 6 단계 산출물을 **사료 (input)** 로 활용
- prod 코드 (현 HEAD) = 6 단계의 **결과물**
- lab fresh v2 = 사료를 정독 + **새 기준에서 재해석** → effizient한 implementation

본 작업의 디시플린은 MASTER_PLAN 의 디시플린을 계승:
- 모든 정독 결과 즉시 본 master doc 박제
- 각 사료 정독 시 본 master doc 의 진행 로그 갱신
- 한 사료 정독 = 한 단위 (대형 사료는 분할)
- 컨텍스트 단절 시 본 master doc 만 보면 재개 가능

---

### S1.1 — `_meta/CHARTER_11TABS_EQUAL_v1.md` (218 줄, v1.1)

**작성**: 2026-05-07 (집 PC). 사용자: "누더기 만들지 맙시다" 명시.
**효력**: 본 헌장 박제 이후 모든 저장·동기화 관련 코드 변경의 기준점.

#### 가치 3 + 우선순위 (헌장의 axiom)
1. **무손실** — 사용자 친 입력 안 사라짐 ★ 최우선
2. **비대칭 0** — 11 탭 같은 계약
3. **다른 사용자 작업 보존**
- 우선순위: **1 ≫ 3**. "충돌 막으려고 저장 자체를 막는 설계는 본말전도" (사고 D `7edf8c0` Forward fix 영역)

#### Dirty 의 명시적 정의
- 사용자 입력 → 데이터 변경 → **dirty=예**
- 그 탭의 PUT 성공 → KV 도달 → **dirty=아니오**
- **fetch / 서버 받기 / 초기 load → dirty 안 만듦** (사용자가 한 게 아니므로)

#### 약속 W (Walls of Durability) — 무손실 4 벽
가치 1 의 보장 = 4 벽 모두 살아있는 것. 한 벽 깨지면 가치 1 자체 깨짐.

| 벽 | 정의 | 트리거 |
|---|---|---|
| **W1** 자동 재시도 | PUT transient failure (5xx/네트워크/타임아웃) backoff (1·2·3 초). 충돌(409)/deleted 는 즉시 W2/충돌 escalate | 시스템 |
| **W2** 로컬 박제 | W1 모두 실패 → 클라 localStorage 자동 박제. 충돌(409) 시에도 박제 (내 데이터 안전망) | 시스템 |
| **W3** 사용자 손에 | JSON 파일 다운로드. 시스템·서버·KV 망가져도 사용자 보유. 수동 저장 실패 모달에 다운로드 버튼 | 사용자 + 시스템 |
| **W4** 떠나기 전 인지 | dirty 있는 상태로 닫기/새로고침/URL 이동 시 beforeunload 알림 | 브라우저 |

**구조적 보장**: W1~W4 = engine 단일 책임으로 구현. 호출자 (App.jsx 등) 는 wiring 분산 X — engine 결과 (저장 상태 / 백업 목록 / 다운로드 트리거) 만 받음. **분산 wiring = 한 곳 빠뜨림으로 가치 1 깨질 위험** (이게 사고 D 의 패턴).

**호환 보존**:
- W2 localStorage 키 형식: `te_backup_{type}_{sessionId}_{timestamp}` (변경 X)
- W3 JSON payload schemaVersion: `"2.0"` (변경 X)

#### 저장 모멘트 보장 (a)(b)(c)
PUT 일어나는 모든 모멘트 (자동 throttle fire / 수동 저장 버튼) 에:
- **(a) 정확성**: dirty 인 모든 탭 모든 변경분 빠짐없이 PUT 에 포함. 11 탭 동등.
- **(b) 비겹침**: baseSavedAt mismatch → 충돌 처리.
  - **같은 sub** 자기 자신 충돌: 자동 통합, ConflictModal 미노출
  - **다른 sub** 충돌: 409 + ConflictModal 노출
- **(c) 부분 실패 격리**: 한 탭 conflict/실패가 다른 탭 PUT 막지 않음

**자동/수동 통일**: 자동 throttle fire 와 수동 저장 버튼 = **동일 PUT 경로**. 두 코드 경로일 수 없음.

#### 1조 — 30초 cascading throttle
- dirty 발생 → 30s timer 시작
- 30s 후 fire → 그 시점 dirty 인 모든 탭 PUT
- PUT 후 dirty 잔존 → 30s timer 새로 시작
- dirty 비면 timer 정지
- 새 dirty → 첫 dirty 부터 시작
- **timer 는 사용자 입력으로만 진행**. 탭 이동/폴링/fetch 영향 X

#### 2조 — 탭 이동이 저장 막지 않음
편집자 탭 이동해도 직전 탭 dirty 는 1조 시계 따라 KV 도달. 이동 자체로 저장 멈추거나 시계 초기화 X.

#### 4조 — 같은 탭 동시 수정 conflict 는 운영 약속
**운영 약속**: "한 사용자가 자신의 어느 디바이스에서든, 다른 사용자가 같은 탭에 동시 들어가지 않음"
- 위반 손실 (30s 윈도우 한쪽 분량 / 자기 다중 디바이스) = **시스템 책임 외**
- 충돌 발견 → ConflictModal 사용자 결정
- **페이지 닫힘 30s 손실도 운영 약속 흡수** — 마지막 작업 후 저장 버튼 + 떠나기 = 사용자 약속

#### 5조 — 11 탭 동등
우선순위/차등 0. 모든 저장 경로 (throttle / 저장 버튼 / fetch / applyServer / lastLoadedAt 추적) = 11 탭 동등 dispatch.

#### 약속 X — 탭 진입 freshness
- 탭 진입 시:
  - dirty=아니오 → KV fresh fetch (화면 = 0초 전 KV)
  - dirty=예 → fetch X (React state 가 더 최신)
- **폴링은 freshness 책임 X**. 폴링의 4 가지 책임만:
  1. heartbeat
  2. active-users 갱신 (단계 A)
  3. 토스트 알림 (다른 편집자 X 탭 수정)
  4. /leave 트리거
- 앱 마운트 = "default 탭 진입" 한 형태. default 탭만 fetch. **prefetch 안 함**.

#### 약속 Y — fetch / 서버 받기 / 초기 load 는 dirty 안 만듦
모든 fetch / 서버 적용 코드 경로는 시작 시점~React state 박힘 직후까지 **dirty 차단 명시 메커니즘** (스위치/플래그). 이름·구조 자유 (예: `justLoadedRef`).

#### 금지 조항 (시스템 차원 강제)
**어떤 저장 경로도 dirty=아니오 인 탭의 데이터를 KV 에 PUT 하지 않음.** 4조 운영 약속을 시스템 차원 강제.

#### UX 결정
- **ConflictModal** (다른 sub):
  - 헤더: "다른 사용자와의 동시 저장이 일어났습니다. / 30초 이내의 변경사항이 일어났습니다."
  - 옵션 2: ① 강제저장 ② 동기화
  - clientMergeTabData 코드 유지, UX 미노출
- **active-users UI** (단계 A):
  ```
  👥 동시 편집:
  - 박편집자 → visual
  - 김편집자 → correction
  ```
  - heartbeat tabs 누적 결함 (N5) 해소 — 사용자 현재 탭 1 개로 교체
  - 단계 B (dirty 정보까지) 미도입

#### 명시적 거부 설계 (헌장이 안 함)
- 클라가 baseSavedAt 정밀도로 저장 게이트 (가치 우선순위 역전, 사고 D)
- 폴링이 11 탭 데이터 동기화
- 운영 약속 위반을 자동 머지로 흡수
- 부모/자식 탭 카테고리 (코드 위치 차이의 잔재)
- 탭 이동이 자동 저장 트리거 (N9 사고 영역)
- 앱 마운트 시 모든 탭 prefetch

#### 시스템 책임 외 영역 (헌장 미적용)
- 같은 탭 동시 수정 30s 윈도우 손실 (4조)
- 자기 다중 디바이스 자기 덮어쓰기 (4조 일반화)
- 마지막 작업 후 저장 안 누르고 닫기 30s 손실 (운영 약속)
- 네트워크 끊김 / 브라우저 크래시 (브라우저 영역)
- 의미적 충돌 자동 머지 (clientMergeTabData UX 비노출)

#### 헌장이 명시한 후속 단계
1. **dispatch 계약 명세** — 11 탭 동등 dispatch 의 정확한 함수 시그니처/입출력/보장
2. **마이그레이션 경로** — 단계별 변경 묶음 + 위험도/의존/검증
3. **검증 시나리오** — A~J + K~N 14 부류 + 자기 race
4. **사용자 명시 승인 후** 코드 적용 (POSTMORTEM 6 룰 + Rule 7)

#### 헌장이 식별한 5+α 비대칭 (코드 평탄화 영역)
1. **dirty 감지** — currentSnapshot 11 탭 (자식 exportCache 포함). N3 결함
2. **throttle dispatch** — saveDirtyTabsToKV 11 탭 PUT
3. **applyServerToState** — ConflictModal "서버 받기" 11 탭 갱신. N1 결함
4. **lastLoadedAt 추적** — 자식 컴포넌트 fetch 시 부모 ref 동기. N2 결함
5. **탭 진입 fetch (약속 X)** — setTab 시점 fetch + lastLoadedAt + 약속 Y 가드
6. **throttle 패턴** — debounce → cascading
- 추가: pagehide sendBeacon, active-users UI, ConflictModal 2 옵션, 자기 sub 자동 통합

---

## 도출되는 fresh v2 architecture (자라며 누적)

CHARTER 본질에서 emerge 하는 architecture 영역 (S1.1 단계 도출):

### A1. Storage Engine (단일 책임 모듈)
**책임**: 약속 W (W1~W4) 통합 관리.
**계약**: 호출자에게 (저장 상태 / 백업 목록 / 다운로드 트리거) 만 노출.
**금지**: 호출자 측 wiring 분산 (사고 D 패턴).
**구성**:
- W1 retry: backoff (1/2/3 초), 5xx/network/timeout 만, 409/deleted 는 즉시 escalate
- W2 localStorage: 자동 박제, 키 `te_backup_{type}_{sessionId}_{timestamp}` (호환 보존)
- W3 download: JSON, schemaVersion `"2.0"` (호환 보존)
- W4 beforeunload: dirty 있을 때만

### A2. Dirty Tracking (단일 진실)
**책임**: 11 탭 dirty 상태 관리.
**규칙**:
- 사용자 입력 → dirty=예
- PUT 성공 → dirty=아니오
- fetch/load → 절대 dirty 안 만듦 (약속 Y 메커니즘)
**금기**: dirty=아니오 인 탭 PUT 절대 금지 (헌장 §3)

### A3. Throttle Engine (1조 cascading)
**책임**: 30s cascading throttle.
**규칙**:
- debounce 아님 (timer reset X)
- 사용자 입력으로만 진행
- 탭 이동/폴링/fetch 영향 받지 X
- PUT 후 dirty 잔존 → 새 cycle
**의존**: A2 (dirty), A4 (PUT 경로)

### A4. Save Path (자동/수동 통일)
**책임**: PUT 의 단일 경로. 자동 throttle fire 와 수동 저장 버튼이 같은 코드.
**보장**: (a) 정확성 / (b) 비겹침 / (c) 부분 실패 격리
**의존**: A1 (W1 retry), A2 (dirty), A6 (conflict)

### A5. Tab Dispatch (11 탭 동등)
**책임**: 모든 탭 작업의 단일 dispatch table.
**모듈 covered**:
- SAVE (saveDirtyTabsToKV)
- APPLY_SERVER (ConflictModal "서버 받기")
- FETCH (탭 진입)
- LAST_LOADED_AT (자식 컴포넌트 fetch 동기)
- DIRTY_SNAPSHOT (currentSnapshot 11 탭)
**금기**: 부모/자식 카테고리 분리 (헌장 §6 거부)

### A6. Conflict Handler
**책임**: baseSavedAt mismatch 처리.
**규칙**:
- 같은 sub: 자동 통합 (UX 미노출, server side 처리)
- 다른 sub: 409 + ConflictModal (옵션 2: 강제저장 / 동기화)
**의존**: server 측 sub 비교 단서, A4 (force 경로)

### A7. Tab Entry Fetch (약속 X)
**책임**: setTab 시점 freshness 보장.
**규칙**:
- dirty=아니오 → fetch
- dirty=예 → skip
- fetch 결과는 약속 Y 메커니즘으로 dirty 차단
**금기**: 폴링이 freshness 책임. 마운트 시 모든 탭 prefetch.

### A8. Polling (4 책임만)
**책임 한정**: heartbeat / active-users / 토스트 / /leave
**금기**: 11 탭 데이터 동기화

### A9. UX Surfaces
- **ConflictModal**: 2 옵션, 정확한 헤더 문구
- **active-users UI**: 단계 A, 사용자 현재 탭 1개
- **save status indicator**: dirty / pending / saving / saved / failed
- **backup list / download trigger**: W3 노출

### A10. Charter Compliance Verification
**책임**: 헌장 §5 의 5+α 비대칭 검증을 자동 (테스트) 또는 명시 (linter / type) 로 시스템 강제.

---

## 진행 로그

### 2026-05-09 KST
- 16:?? — 사용자 방향 전환 (코드 복사 → fresh v2 설계). 옛 ops 사료 superseded
- 16:?? — 본 master doc 신설
- 17:?? — Stage 1 시작. 사료 우선순위 1번 (CHARTER) 정독 완료
- 17:?? — CHARTER 본질 박제 (S1.1 섹션) + 도출 architecture (A1~A10) 박제
- 17:?? — 우선순위 2번 (00_MASTER_PLAN) 정독 완료. S1.2 박제 (6 단계 흐름 + 188 .md 의 조직 구조 + 디시플린)
- 17:?? — 주기 commit (S1.1 + S1.2 박제 보존)
- (이하 사료 정독 + 박제 진행하며 누적)

## 다음 세션 / 다음 단계 재개 지점

본 master doc 만 읽으면 재개 가능. 현재 위치:
- ✓ Pivot 완료 (commit `f2aff94`)
- ✓ S1.1 CHARTER essence 박제
- ✓ S1.2 MASTER_PLAN essence + 188 .md 조직 구조 박제
- ▶ **다음**: `_meta/PROGRESS.md` (348 줄) — 실 진행 사료. + KEY_FINDINGS / RESUME_GUIDE / SESSION_HANDOFF / POSTMORTEM / AUDIT 시리즈

남은 사료 우선순위 (양):
- _meta/ 안 — POSTMORTEM (527) / MULTIUSER (547) / PROGRESS (348) / SESSION_HANDOFF (216) / KEY_FINDINGS (197) / AUDIT v1 (285) v2 (99) v3 (185)
- 03_PRD/revised_PRD.md (1235) ← 사료 중 가장 큼
- 06_v2_finalization 5 개 (596 + 430 + 123 + 176 + 113 ≈ 1438)
- HANDOFF / RESUME_AT_*_PC (1404)
- 01-05 단계 산출물 ~ 150 .md (대다수 가벼움)
- archive/ + reports/ + ttimes-* 8 폴더 + 06_v2_finalization/code/ + reviews/

총합 ~ 5000-7000 줄 핵심 사료 + α. 다 세션 작업 그대로 유효.

## 참조

- prod editor 위치: `D:\OneDrive - 머니투데이\개발\통합 개발\editor\`
- prod editor HEAD: `0b4711d` (Phase 1.1 commit)
- prod editor CHANGELOG: 이 문서의 "옛 사료" 섹션 참조
- 사료 폴더: `D:\OneDrive - 머니투데이\개발\통합 개발\cms-v2-plan\` (188 개 .md)
- 8 옛 앱 폴더: `D:\…\통합 개발\ttimes-{admin,hilight,main,modify,setgen,subtitle,visual,you}\`
