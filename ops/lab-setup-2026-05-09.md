# LAB-SETUP 2026-05-09 — test 환경 재설계 (lab 신설)

## 배경

이번 세션 prod editor v2 안정화 (commits `9a6cef0` ~ `15b5bd1`, 13 건 + CHANGELOG #1~#15) 결과 prod 가 stable. 사용자 명시: "**현재 editor(prod) 의 상태가 상당히 stable. v2 의 시금석으로 삼아 test 도 prod v2 기반으로**".

옛 test 측 (ttimesvibe 의 worker `editor` + KV `editor-sessions` + repo `ttimesvibe/ttimes-editor`) 의 핵심 결함:

1. **"editor" 단어 충돌**: prod 로컬 폴더 (`editor/`) + test worker 이름 (`editor`) — "editor 어디" 라고 하면 모호. 사용자 지적: "editor라는 단어가 중간에 겹치면 지금이랑 뭐가 다른거죠?"
2. **5/6 코드 stale**: 이번 세션 prod 12 commit (worker 2 회 배포 포함) 미반영 = test 가 prod 보다 옛 버전

→ test 환경 = `lab` 으로 재설계. 로컬/GitHub/worker/KV/Pages 모든 레벨에 "editor" 단어 0.

## 사용자 결정 매트릭스

| # | 결정 |
|---|---|
| 이름 | **`lab`** (옵션 2) — prod editor 와 단어 충돌 0 |
| B | **KV 데이터 마이그레이션** (90 keys 전부 — `editor-sessions` → `lab-sessions`) |
| C | GitHub `ttimesvibe/ttimes-editor` 삭제 (Phase 3) |
| D | 옛 worker `editor` (test) 폐기 — Phase 3 (lab 검증 후) |
| E | 옛 worker `ttimes-edit` (4/12 죽음, 500 응답) 폐기 — Phase 3 |
| F | 로컬 위치 `D:\OneDrive - 머니투데이\개발\통합 개발\lab\` (OneDrive 안 — 사용자 의견: "확연히 이름이 다른데 왜 사고?") |
| G | OPENAI/GEMINI 키 = prod 와 공유. **JWT_SECRET 별도** (test 토큰이 prod 와 안 섞이게) |
| H | Pages = prod 동일 형식 (docs/ 빌드 → GitHub Pages) |
| I | lab repo 첫 baseline = prod editor HEAD (`3ab1184` + CHANGELOG `15b5bd1`) |
| J | 옛 KV 정리 (editor-sessions / ttimes-editor-sessions / SESSIONS / prod 이름) — Phase 3, "복잡하니 맨 마지막" |

## Phase 0 — 사용자 직접 수행 (완료)

- [x] 로컬 `ttimes-editor-test/` → `lab/` rename + 콘텐츠 비우기 (`.git` 까지 모두 삭제)
- [x] GitHub repo `ttimesvibe/lab` 신설
- [x] PAT "cms lab" 발급 (Repository: `ttimesvibe/lab` only, Contents: Read/write) — 작업 후 폐기 예정
- [x] Cloudflare Worker `lab` 신설 (URL: `lab.ttimes.workers.dev`)
- [x] KV namespace `lab-sessions` 신설 (id `fbb8da8adcae4ee0a555abff66f798ac`)
- [x] Worker → KV binding 이름 정정 (옛 `lab` → `SESSIONS`, prod 코드 호환 위해)

## Phase 1 — 코드 + 인프라 셋업 (이번 작업)

진행 단계 (각 단계 완료 시 [x] 표시 + 결과 박제):

### 1. lab/ 폴더 비어있음 검증 + ops 사료 폴더 신설
- [ ] lab/ 폴더 빈 상태 확인 (사용자 (가) 수행 후 검증)
- [ ] `editor/ops/` 폴더 신설 + `README.md` + 본 문서
- [ ] `editor/CLAUDE.md` 에 lab 섹션 추가
- [ ] `editor/CHANGELOG.md` 에 #16 entry 추가
- [ ] commit + push (사용자 명시 허락 후) — = "lab 작업 시작 박제"

### 2. prod editor HEAD 코드 → lab/ 복사
- 대상: `3ab1184` + CHANGELOG `15b5bd1` (HEAD)
- 방법: 파일 단위 복사 (`.git` 제외, lab repo 의 새 git history 보존)
- 복사 대상: `docs/` `worker/` `README.md` `CLAUDE.md` (사료 참조용?) — 검토 후 결정
- [ ] 미진행

### 3. wrangler.toml lab 전용 설정
- `name="lab"`
- `account_id=fb0a10864393158e940b149b3ead37f6` (ttimesvibe)
- KV id `fbb8da8adcae4ee0a555abff66f798ac` (lab-sessions)
- `[triggers] crons = []` (cron 없음, prod B 안 정합)
- [ ] 미진행

### 4. config.js / build.js workerUrl → `lab.ttimes.workers.dev`
- `docs/src/utils/config.js` 의 `workerUrl`
- `docs/build.js` 의 `CANONICAL_WORKER_URL`
- 양쪽 동시 변경 (drift guard 만족)
- [ ] 미진행

### 5. Worker secrets 셋업
- `OPENAI_API_KEY` (prod 와 같은 값 — 사용자가 직접 `wrangler secret put` 입력)
- `GEMINI_API_KEY` (prod 와 같은 값 — 사용자가 직접)
- `JWT_SECRET` (별 키 — 임의 생성된 hex 64 char, 사용자가 직접 입력)
- secret 값은 chat 노출 X (사용자 직접 입력 필수)
- [ ] 미진행

### 6. Build (npm run build)
- prod editor 의 `build.js` drift guard 통과 확인
- 산출 번들 `lab/docs/assets/` 에 생성
- [ ] 미진행

### 7. git init + remote + 첫 commit + push
- `cd lab && git init -b main`
- `git remote add origin https://x-access-token:<PAT>@github.com/ttimesvibe/lab.git`
- 첫 commit: `Initial: lab baseline from prod editor HEAD (3ab1184 + 15b5bd1)`
- `git push -u origin main`
- [ ] 미진행

### 8. wrangler deploy lab worker
- `cd lab/worker && npx wrangler deploy`
- 응답: `lab.ttimes.workers.dev` 의 새 코드 적용 (Cloudflare Version 신규 발급)
- [ ] 미진행

### 9. KV 데이터 마이그레이션 (`editor-sessions` 90 keys → `lab-sessions`)
- list keys (90 개)
- 각 key 별 get value + expiration
- bulk put to lab-sessions (TTL 보존)
- 검증: lab-sessions 키 수 = 90
- [ ] 미진행

### 10. GitHub Pages 활성
- `ttimesvibe/lab` Settings → Pages → main branch /docs 폴더
- URL: `ttimesvibe.github.io/lab/`
- [ ] 미진행

### 11. 라이브 검증
- `lab.ttimes.workers.dev/health` 응답 (인증 require → 401 정상)
- `ttimesvibe.github.io/lab/` Pages 접근
- 사용자 시연 (project 진입 / 저장 / 일정 등록 / 캘린더 등 — prod 와 동일 동작)
- [ ] 미진행

### 12. CHANGELOG #16 박제 + 본 문서 종결
- `editor/CHANGELOG.md` #16 entry "Phase 1 완료" 갱신
- 본 문서 "Phase 1 완료" 표시
- 후속 작업 (Phase 3 deferred) 명시
- [ ] 미진행

## Phase 2 — 라이브 검증 (사용자 시연)

Phase 1 끝난 후 사용자가 lab 환경 사용해보면서 prod 와 동일 동작 검증.

## Phase 3 — 옛 환경 정리 (deferred — 사용자 명시 "맨 마지막")

| # | 항목 | 비고 |
|---|---|---|
| D | 옛 worker `editor` (ttimesvibe) 삭제 | lab 검증 후 |
| E | 옛 worker `ttimes-edit` 삭제 | 즉시 가능 (이미 죽음) |
| C | GitHub repo `ttimesvibe/ttimes-editor` 삭제 | 또는 archive |
| J-1 | KV `editor-sessions` 삭제 | 마이그레이션 완료 후 |
| J-2 | KV `ttimes-editor-sessions` 삭제 | 레거시 8 keys |
| J-3 | KV `SESSIONS` (제네릭) | 조사 + 폐기 결정 |
| J-4 | 다른 서비스 KV (hilight/modify/subtitle/visual) | 별 작업 (본 lab 무관) |
| J-5 | prod KV `editor-session` 이름 변경 결정 | (선택) |
| 보안 | 사용자 PAT "cms lab" 폐기 (Regenerate or Delete) | Phase 1 끝나면 즉시 |

## 진행 로그 (사고/사건/결정 시각순)

### 2026-05-09 (KST)
- 14:03:54 — OneDrive 동기화 사고 (CHANGELOG #15 별 사건, prod editor 측)
- 16:00 즈음 — 사용자가 `ttimes-editor-test/` → `lab/` 폴더 rename 시작
- 16:21 — `lab/` 안에 `.git` 만 남음 확인 → 사용자 결정 (가) `.git` 삭제
- 16:?? — 사용자 `.git` 삭제 완료. lab/ 완전 빈 폴더
- 16:?? — 본 사료 + CLAUDE.md + CHANGELOG.md 신설/갱신 — = "lab 작업 시작 박제" commit
- (Phase 1 단계별 진행 시 박제 추가)

## 참조

- prod editor: `D:\OneDrive - 머니투데이\개발\통합 개발\editor\` (HEAD `3ab1184` + CHANGELOG `15b5bd1`)
- prod editor CHANGELOG: `editor/CHANGELOG.md` (#1~#15)
- prod editor 운영 메모: `editor/CLAUDE.md`
- 옛 test 사료: `D:\…\통합 개발\ttimes-editor-test\` (사용자가 lab/ 으로 rename + 비움 → 콘텐츠 사라짐)
- 옛 test GitHub: `github.com/ttimesvibe/ttimes-editor` (Phase 3 에서 삭제 예정)
