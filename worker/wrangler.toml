name = "ttimes-edit"
main = "index.js"
compatibility_date = "2024-01-01"

# ⚠️ [placement] 절대 추가 금지!
# wrangler deploy 시 대시보드 Region 설정이 덮어씌워짐
# Region은 Cloudflare 대시보드에서 수동 설정 (GCP US)

[[kv_namespaces]]
binding = "SESSIONS"
id = "b4c4e3c3944146cb856b2622fff59473"   # ttimes-editor-sessions

# Secrets (이미 설정됨, wrangler secret put 으로 변경 가능):
# - OPENAI_API_KEY
# - GEMINI_API_KEY
